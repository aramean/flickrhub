const id = 'form';
const placeholderText = 'Search flickr photos ...';
const submitText = 'Search';

export const form = `
  <form id="${id}">
    <input type="text" placeholder="${placeholderText}"><input type="submit" value="${submitText}">
  </form>
`;